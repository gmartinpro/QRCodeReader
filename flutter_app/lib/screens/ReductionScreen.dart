import 'package:flutter/material.dart';

class ReductionScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Reduction"),
      ),
          body:Container(
        alignment: Alignment.center,
        child: Text("ReductionScreen"),
      ), 
    );
  }
}